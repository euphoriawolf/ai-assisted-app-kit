{
  "name": "@app/queue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:standalone": "wrangler dev --port 8788 --persist-to .wrangler/state",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@app/db": "workspace:*",
    "@app/shared": "workspace:*",
    "drizzle-orm": "^0.44.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.22.0"
  }
}
